import 'package:flutter/material.dart';

void main() {
  runApp(const RegressionApp());
}

class RegressionApp extends StatelessWidget {
  const RegressionApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
